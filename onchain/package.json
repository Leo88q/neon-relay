{
  "name": "@neonrelay/onchain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Neon Relay Anchor program: client-side Merkle helpers and offline tests (stage 9). Zero runtime dependencies.",
  "scripts": {
    "test": "node --experimental-strip-types --test \"test/**/*.test.ts\"",
    "typecheck": "node --experimental-strip-types scripts/typecheck.ts",
    "verify:ids": "node scripts/verify_source_ids.mjs",
    "verify:toolchain": "node scripts/verify_toolchain_pin.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
